# 行尾一律 LF —— 為了讓 SHA256 稽核跨機器可重現
#
# 本 repo 在公司機（Windows，core.autocrlf=true）與家用機之間同步，而專案的
# 程式碼檔一律走 code-writer → code-qa → code-reviewer 三 agent 流程，writer 的
# Manifest 會宣告每個檔案的 SHA256、QA 逐字元比對。
#
# 沒有本檔時，Windows 端 checkout 會把 LF 轉成 CRLF，檔案位元組數隨即改變、
# SHA256 跟著漂移，但 git 認定內容零差異 —— 於是「hash 對不上」會被誤判成竄改。
# 2026-07-29 QA 實測踩到：git checkout -- 之後 mitake.py 從 47867 變 48915 bytes。
#
# deploy/*.service 另有一層理由：systemd unit 帶 CRLF 會解析失敗。

*.py        text eol=lf
*.md        text eol=lf
*.txt       text eol=lf
*.service   text eol=lf
*.example   text eol=lf
*.toml      text eol=lf
*.cfg       text eol=lf
*.ini       text eol=lf
*.mmd       text eol=lf
.gitignore  text eol=lf
.gitattributes text eol=lf

# 二進位檔不做任何行尾轉換
*.png       binary
*.jpg       binary
*.pdf       binary
*.m4a       binary
*.pptx      binary
